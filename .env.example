# ---- AI (server-side only; never prefix with NEXT_PUBLIC_) ----
# Free key from https://aistudio.google.com/apikey (no billing needed).
GEMINI_API_KEY=
# Optional: override the Gemini model (defaults to gemini-3.7-flash).
# EUROCV_MODEL=gemini-3.7-flash

# Public site URL used for metadata and sitemap.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ---- Accounts (Supabase) — optional; without these the app runs anonymously ----
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Set to "true" once the Google provider is configured in Supabase Auth.
NEXT_PUBLIC_AUTH_GOOGLE=false
# Set to "true" when the magic-link email template includes {{ .Token }}.
NEXT_PUBLIC_AUTH_EMAIL_CODE=false
